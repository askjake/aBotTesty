export const APP_ENV = process.env.APP_ENV || 'local';

export const BACKEND_URL = `${process.env.NEXT_PUBLIC_BACKEND_URL}/rest/api/v1`;

export const BETA_REPORTS_URL = `${process.env.NEXT_PUBLIC_ROOT_FRONTEND_URL}/beta-reports`;

export const NODE_ENV = process.env.NODE_ENV;
