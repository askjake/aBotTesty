import styled from 'styled-components';
import { Sender } from '@ant-design/x';

export const StyledSenderHeaderBlockContainer = styled(Sender.Header)`
  &.ant-sender-header .ant-sender-header-content {
    padding: 0;
  }
`;
