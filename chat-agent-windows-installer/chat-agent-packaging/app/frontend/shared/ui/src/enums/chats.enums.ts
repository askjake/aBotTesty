export enum RoleEnum {
  USER = 'user',
  ASSISTANT = 'assistant',
}

export enum AttachmentStatusEnum {
  QUEUED = 'queued',
  PROCESSING = 'processing',
  READY = 'ready',
  FAILED = 'failed',
}

export enum ChatStatusEnum {
  NORMAL = 'normal',
  READONLY = 'readonly',
}
