import styled from 'styled-components';
import { Welcome } from '@ant-design/x';

export const StyledWelcomeBlock = styled(Welcome)`
  &.ant-welcome {
    max-width: 30rem;
    margin: 0 auto;
  }
`;
